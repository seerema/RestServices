# Disable banner
spring.main.banner-mode=off

# Home directory with configuration
# Must be same value as TestConstants.WORK_SBS_SHARED
home.dir=target/sbs_shared

# Root context path
server.servlet.context-path=/web_test

# Application port.
# TODO Switch on TestRestTemplate
server.port=8090

# Default configuration parameters for ws_cfg
test.resy.config.debug=false

# Default configuration parameters for web_cfg
test.ui.config.param1=one
test.ui.config.param2=two
test.ui.config.param3=three

# Set Test debug level 
# logging.level.Test=DEBUG
# logging.level.org.springframework.web=DEBUG

# Data for Actuator /info endpoints
# info.app.version=@project.version@
